import * as Types from "types";

type Props = {
  // color: string;
  // width: string;
  // disabled?: boolean;
  // children: React.ReactNode;
  // onClick: () => void;
} & Types.Commons.Props;

export type { Props };
